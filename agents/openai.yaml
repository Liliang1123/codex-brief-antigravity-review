interface:
  display_name: "Codex Brief / Antigravity Review"
  short_description: "Write read-only prompts or review handed-off evidence"
  default_prompt: "Use $codex-brief-antigravity-review only for non-state-changing prompt wording, ordinary read-only review, or a batch with an existing valid Handoff Contract; return file edits, review-and-fix, workflow changes, and final completion decisions to openspec-superpower-change."

policy:
  allow_implicit_invocation: true
